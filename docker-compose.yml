name: lkjmcrs

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["serve"]
    volumes:
      - ./config:/app/config:ro
      - server-data:/app/data
    ports:
      - "${HOST_PORT:-25565}:25565"

volumes:
  server-data:
